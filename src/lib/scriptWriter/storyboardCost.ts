/**
 * Approximate cost per generated storyboard still (Gemini 2.5 Flash Image).
 * Client-safe constant (no server-only imports) so UI can show cost estimates.
 * Used to show the user a cost estimate before a "Generate all" batch.
 */
export const STORYBOARD_IMAGE_COST_USD = 0.04;
