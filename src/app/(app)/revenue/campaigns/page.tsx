import { RevenuePlaceholderPage } from "@/components/revenue/RevenuePlaceholderPage";

export default function RevenueCampaignsPage() {
  return (
    <RevenuePlaceholderPage
      title="Campaigns"
      subtitle="Create IMG client and Stormi brand prospecting campaigns."
      phaseNote="Campaign forms and research triggers arrive in Phase 2–4."
    />
  );
}
