import { RevenuePlaceholderPage } from "@/components/revenue/RevenuePlaceholderPage";

export default function RevenueProposalsPage() {
  return (
    <RevenuePlaceholderPage
      title="Proposals"
      subtitle="Draft proposals linked to ShootSpine agreements and quotes."
      phaseNote="Extends existing agreement tools in Phase 7."
    />
  );
}
