import { RevenuePlaceholderPage } from "@/components/revenue/RevenuePlaceholderPage";

export default function RevenueInboxPage() {
  return (
    <RevenuePlaceholderPage
      title="Inbox"
      subtitle="Gmail threads, reply classification, and AI email receptionist."
      phaseNote="Gmail integration arrives in Phase 6."
    />
  );
}
